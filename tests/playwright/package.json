{
  "name": "mod_vimipad-playwright",
  "private": true,
  "description": "Browser/collaboration end-to-end tests for mod_vimipad (CI/live only).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
